// import express from "express";
// const WishRoutes = express.Router()
// import {getWishlistByUserId,favorites,favoritesProduct} from "../controller/Wishproduct.js"

// WishRoutes.get("/wishgproduct/:id",getWishlistByUserId) 
// WishRoutes.post("/favorites",favorites );
// WishRoutes.delete("/favorites/:productId",favoritesProduct);

// export default WishRoutes;